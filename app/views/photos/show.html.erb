<h1>Photo</h1>

<div class="photo">
  <%= image_tag @photo.filename %>
  <p><%= @photo.name %></p>
</div>

<% if can? :update, @photo %>
	<%= link_to 'Edit', edit_photo_path(@photo) %> | 
<% end %>
<%= link_to 'Back', photos_path %><br/><br/>

<%= render "comments/comments" %>
<%= render "comments/form" %>