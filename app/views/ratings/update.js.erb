$("#notice").html("<%= flash[:notice] %>");
$('#star').raty({
  readOnly: true,
  score: <%= @article.average_rating %>,
  path: '/assets'
});

$('#user_star').raty({
  score: <%= @rating.score %>,
  path: '/assets',
  click: function(score, evt) {
    $.ajax({
      url: '/ratings/' + <%= @rating.id %>,
      type: 'PUT',
      data: { score: score }
    }).done(function (data){});
  }
});
